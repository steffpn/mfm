---
phase: 09-notifications-station-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - apps/ios/myFuckingMusic/Services/APIEndpoint.swift
  - apps/ios/myFuckingMusic/Models/CompetitorModels.swift
  - apps/ios/myFuckingMusic/ViewModels/CompetitorListViewModel.swift
  - apps/ios/myFuckingMusic/ViewModels/CompetitorDetailViewModel.swift
  - apps/ios/myFuckingMusic/Views/Competitors/CompetitorListView.swift
  - apps/ios/myFuckingMusic/Views/Competitors/CompetitorDetailView.swift
  - apps/ios/myFuckingMusic/Views/Competitors/CompetitorStationPickerView.swift
  - apps/ios/myFuckingMusic/Views/Settings/SettingsView.swift
autonomous: false
requirements:
  - STIN-01
  - STIN-02

must_haves:
  truths:
    - "Station-role user sees 'Competitor Stations' row in Settings"
    - "Non-STATION role users do not see the Competitor Stations row"
    - "User can add competitor stations from a browseable station list (up to 20)"
    - "User can remove competitor stations via swipe-to-delete"
    - "Competitor list shows cards with station name, play count, and top song preview"
    - "Tapping a card opens station detail with top songs, recent detections, and play comparison"
    - "Day/Week/Month segmented control changes the data period"
  artifacts:
    - path: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorListView.swift"
      provides: "Cards layout with competitor station summaries"
      min_lines: 50
    - path: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorDetailView.swift"
      provides: "Station detail with top songs, recent detections, comparison"
      min_lines: 60
    - path: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorStationPickerView.swift"
      provides: "Station browser for adding competitors"
      min_lines: 30
    - path: "apps/ios/myFuckingMusic/ViewModels/CompetitorListViewModel.swift"
      provides: "Competitor list data loading and management"
      min_lines: 40
    - path: "apps/ios/myFuckingMusic/ViewModels/CompetitorDetailViewModel.swift"
      provides: "Station detail data loading"
      min_lines: 30
    - path: "apps/ios/myFuckingMusic/Models/CompetitorModels.swift"
      provides: "Codable models for competitor API responses"
      min_lines: 20
  key_links:
    - from: "apps/ios/myFuckingMusic/Views/Settings/SettingsView.swift"
      to: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorListView.swift"
      via: "NavigationLink visible only for STATION role"
      pattern: "CompetitorListView"
    - from: "apps/ios/myFuckingMusic/ViewModels/CompetitorListViewModel.swift"
      to: "apps/ios/myFuckingMusic/Services/APIClient.swift"
      via: "GET /competitors/summary and /competitors/watched"
      pattern: "competitorSummary|watchedStations"
    - from: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorListView.swift"
      to: "apps/ios/myFuckingMusic/Views/Competitors/CompetitorDetailView.swift"
      via: "NavigationLink on card tap"
      pattern: "CompetitorDetailView"
---

<objective>
iOS competitor station intelligence views: competitor list with cards, station detail drill-down, and station picker for managing watched competitors.

Purpose: Gives station-role users the ability to monitor competitor stations' programming directly from the iOS app. They can see what competitors are playing, compare play counts, and track competitor activity over time.

Output: CompetitorListView (cards), CompetitorDetailView (top songs, recent detections, comparison), CompetitorStationPickerView (add/remove), CompetitorModels, ViewModels, updated SettingsView with role-gated Competitor Stations row.
</objective>

<execution_context>
@/Users/stefanpanaite/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stefanpanaite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-notifications-station-intelligence/09-CONTEXT.md
@.planning/phases/09-notifications-station-intelligence/09-RESEARCH.md
@.planning/phases/09-notifications-station-intelligence/09-02-SUMMARY.md

<interfaces>
<!-- Key types from existing iOS codebase and plan 02 backend API. -->

From apps/ios/myFuckingMusic/Services/AuthManager.swift:
```swift
@MainActor @Observable
final class AuthManager {
    var isAuthenticated = false
    var currentUser: AuthUser?
}
// AuthUser has .role: String property
```

From apps/ios/myFuckingMusic/Views/Settings/SettingsView.swift:
```swift
struct SettingsView: View {
    @Environment(AuthManager.self) private var authManager
    // Plan 03 will have already added "Notifications" NavigationLink
    // This plan adds "Competitor Stations" NavigationLink (STATION role only)
}
```

From apps/ios/myFuckingMusic/Views/Dashboard/DashboardView.swift (segmented control pattern):
```swift
// Uses Picker with .segmented style for Day|Week|Month selection
// Uses TimePeriod enum: .day, .week, .month with rawValue strings
```

From apps/ios/myFuckingMusic/Views/Detections/DetectionRowView.swift:
```swift
struct DetectionRowView: View {
    let detection: AirplayEvent  // Reuse for competitor recent detections
}
```

Backend API from plan 02:
- GET /api/v1/competitors/watched -> [{ id, stationId, stationName }]
- POST /api/v1/competitors/watched <- { stationId } -> 201
- DELETE /api/v1/competitors/watched/:stationId -> 204
- GET /api/v1/competitors/summary?period=day|week|month -> [{ stationId, stationName, playCount, topSong }]
- GET /api/v1/competitors/:stationId/detail?period=day|week|month -> { topSongs, recentDetections, comparison }
- GET /api/v1/stations -> [{ id, name, ... }] (existing endpoint for station picker)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Competitor models, API endpoints, and ViewModels</name>
  <files>
    apps/ios/myFuckingMusic/Models/CompetitorModels.swift,
    apps/ios/myFuckingMusic/Services/APIEndpoint.swift,
    apps/ios/myFuckingMusic/ViewModels/CompetitorListViewModel.swift,
    apps/ios/myFuckingMusic/ViewModels/CompetitorDetailViewModel.swift
  </files>
  <action>
    1. **CompetitorModels** (`Models/CompetitorModels.swift`):
       - `WatchedStation`: Codable, Sendable, Identifiable struct with `id: Int`, `stationId: Int`, `stationName: String`
       - `CompetitorCard`: Codable, Sendable, Identifiable struct with `stationId: Int`, `stationName: String`, `playCount: Int`, `topSong: CompetitorTopSong?`. Identifiable via stationId.
       - `CompetitorTopSong`: Codable, Sendable struct with `title: String`, `artist: String`
       - `CompetitorDetail`: Codable, Sendable struct with `topSongs: [CompetitorSong]`, `recentDetections: [CompetitorDetection]`, `comparison: [CompetitorComparison]`
       - `CompetitorSong`: Codable, Sendable, Identifiable struct with `title: String`, `artist: String`, `isrc: String?`, `playCount: Int`. Identifiable via UUID().
       - `CompetitorDetection`: Codable, Sendable, Identifiable struct with `id: Int`, `songTitle: String`, `artistName: String`, `startedAt: String`
       - `CompetitorComparison`: Codable, Sendable, Identifiable struct with `songTitle: String`, `artistName: String`, `theirPlays: Int`, `yourPlays: Int`. Identifiable via UUID().
       - `AddWatchedStationRequest`: Encodable struct with `stationId: Int`

    2. **APIEndpoint additions** (`Services/APIEndpoint.swift`):
       - NOTE: Plan 03 may have already added notification endpoints. Merge carefully with existing cases.
       - Add cases: `watchedStations`, `addWatchedStation(stationId: Int)`, `removeWatchedStation(stationId: Int)`, `competitorSummary(period: String)`, `competitorDetail(stationId: Int, period: String)`
       - Paths: /competitors/watched (GET), /competitors/watched (POST), /competitors/watched/{stationId} (DELETE), /competitors/summary (GET), /competitors/{stationId}/detail (GET)
       - Methods: GET for list/summary/detail, POST for add, DELETE for remove
       - Body: AddWatchedStationRequest for addWatchedStation
       - QueryItems: period for summary and detail
       - requiresAuth: all true

    3. **CompetitorListViewModel** (`ViewModels/CompetitorListViewModel.swift`):
       - @Observable class with @MainActor
       - Properties: `cards: [CompetitorCard] = []`, `watchedStations: [WatchedStation] = []`, `selectedPeriod: String = "day"`, `isLoading = false`, `error: String?`
       - `loadSummary()`: Fetch GET /competitors/summary?period={selectedPeriod}. Update cards array. Handle empty state (no watched stations).
       - `addStation(stationId: Int)`: POST /competitors/watched. On success, reload summary. Handle 409 (already watched), 400 (max reached, own station).
       - `removeStation(stationId: Int)`: DELETE /competitors/watched/{stationId}. On success, remove from local cards array and reload.
       - `loadWatchedStations()`: GET /competitors/watched. Update watchedStations array (used by picker to know which are already watched).

    4. **CompetitorDetailViewModel** (`ViewModels/CompetitorDetailViewModel.swift`):
       - @Observable class with @MainActor
       - Properties: `stationId: Int`, `stationName: String`, `selectedPeriod: String = "day"`, `detail: CompetitorDetail?`, `isLoading = false`, `error: String?`
       - Init takes stationId and stationName
       - `loadDetail()`: Fetch GET /competitors/{stationId}/detail?period={selectedPeriod}. Update detail.
       - Reload when selectedPeriod changes (via .task(id: selectedPeriod) in view).
  </action>
  <verify>
    <automated>cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic && xcodebuild -scheme myFuckingMusic -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CompetitorModels defines all Codable types for competitor API responses
    - APIEndpoint has all competitor endpoint cases with correct paths, methods, query params
    - CompetitorListViewModel manages cards, period selection, add/remove stations
    - CompetitorDetailViewModel loads detail for a single competitor station
    - Project compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: CompetitorListView, CompetitorDetailView, StationPickerView, and SettingsView integration</name>
  <files>
    apps/ios/myFuckingMusic/Views/Competitors/CompetitorListView.swift,
    apps/ios/myFuckingMusic/Views/Competitors/CompetitorDetailView.swift,
    apps/ios/myFuckingMusic/Views/Competitors/CompetitorStationPickerView.swift,
    apps/ios/myFuckingMusic/Views/Settings/SettingsView.swift
  </files>
  <action>
    1. **CompetitorListView** (`Views/Competitors/CompetitorListView.swift`):
       - @State private var viewModel = CompetitorListViewModel()
       - @State private var showingStationPicker = false
       - NavigationStack wrapping (if not already provided by SettingsView NavigationLink)
       - Top: Picker for Day | Week | Month with .segmented style (same pattern as DashboardView)
       - Cards layout: ScrollView with LazyVStack. Each card shows:
         - Station name (bold, larger text)
         - Play count: "X plays" in secondary text
         - Top song preview: "'Song Title' by Artist" in caption style, or "No plays" if topSong is null
       - Cards should be tappable (NavigationLink to CompetitorDetailView)
       - Swipe to delete on cards (swipeActions with .destructive role calling viewModel.removeStation)
       - Empty state when no competitors: VStack with icon (binoculars or similar), "No Competitor Stations", "Tap + to start monitoring competitor stations", centered
       - Toolbar: .toolbar { ToolbarItem(placement: .topBarTrailing) { Button(systemImage: "plus") { showingStationPicker = true } } }
       - .sheet(isPresented: $showingStationPicker) { CompetitorStationPickerView(viewModel: viewModel) }
       - .task(id: viewModel.selectedPeriod) { await viewModel.loadSummary() }
       - .navigationTitle("Competitors")
       - Handle loading/error with LoadingView/ErrorView

    2. **CompetitorDetailView** (`Views/Competitors/CompetitorDetailView.swift`):
       - Init takes stationId and stationName
       - @State private var viewModel: CompetitorDetailViewModel (init in .init or body)
       - Picker for Day | Week | Month segmented control
       - Three sections in a ScrollView/List:
         a) **Top Songs** section: Numbered list (1., 2., 3...) with song title, artist name, play count badge. Empty state: "No songs detected"
         b) **Recent Detections** section: List of detection rows showing song title, artist, timestamp (formatted with DateFormatters). Reuse DetectionRowView pattern if compatible, otherwise create a simpler CompetitorDetectionRow. Limit visible to ~20. Empty state: "No recent detections"
         c) **Play Count Comparison** section: Each row shows song title/artist with two columns: "Theirs: X" and "Yours: Y". Color code -- green if yours > theirs, red if theirs > yours. Empty state: "No overlapping songs"
       - .task(id: viewModel.selectedPeriod) { await viewModel.loadDetail() }
       - .navigationTitle(stationName)
       - Handle loading/error states

    3. **CompetitorStationPickerView** (`Views/Competitors/CompetitorStationPickerView.swift`):
       - @Environment(\.dismiss) private var dismiss
       - Input: CompetitorListViewModel (to call addStation and check already-watched)
       - @State var stations: [Station] = [] (load all stations via GET /stations)
       - @State var searchText = ""
       - SearchableList: List of all stations, filterable by name via .searchable modifier
       - Each row shows station name. If already in viewModel.watchedStations, show checkmark and disable tap.
       - On tap (non-watched): call viewModel.addStation(stationId:). Show brief loading indicator. On success, show checkmark. Handle errors (show alert for max reached, own station, already watched).
       - .task { load stations from GET /stations; await viewModel.loadWatchedStations() }
       - .navigationTitle("Add Competitor")
       - Navigation bar: Cancel button to dismiss

    4. **Update SettingsView** (`Views/Settings/SettingsView.swift`):
       - After the "Notifications" NavigationLink (added by plan 03), add a conditional "Competitor Stations" row:
         ```swift
         if authManager.currentUser?.role.uppercased() == "STATION" {
             NavigationLink {
                 CompetitorListView()
             } label: {
                 Label("Competitor Stations", systemImage: "antenna.radiowaves.left.and.right")
             }
         }
         ```
       - Place this in the "App" section alongside the Notifications link
       - This row is ONLY visible when the user's role is STATION (per locked decision)
  </action>
  <verify>
    <automated>cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic && xcodebuild -scheme myFuckingMusic -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CompetitorListView shows cards with station name, play count, top song for each watched competitor
    - Cards are tappable to drill into CompetitorDetailView
    - Swipe-to-delete removes a competitor station
    - + button opens CompetitorStationPickerView as a sheet
    - CompetitorStationPickerView lets user browse and add stations (with search)
    - CompetitorDetailView shows top songs, recent detections, and play count comparison
    - Day/Week/Month segmented control works on both list and detail views
    - Empty states displayed when no competitors or no data
    - SettingsView shows "Competitor Stations" only for STATION role users
    - Project compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify iOS competitor UI</name>
  <files>apps/ios/myFuckingMusic/Views/Settings/SettingsView.swift</files>
  <action>
    Human verification of iOS competitor station intelligence UI. Build and run the app in iOS Simulator to verify:
    1. Log in with a STATION-role test account -- verify "Competitor Stations" row appears in Settings
    2. Tap "Competitor Stations" -- verify empty state with message and + button
    3. Tap + -- verify station picker sheet with searchable station list
    4. Select a station -- verify it appears as a card with name, play count, top song
    5. Tap card -- verify detail view with Day|Week|Month segmented control and three sections
    6. Swipe-delete a competitor -- verify removal
    7. Log in with ARTIST role -- verify "Competitor Stations" row is NOT visible
  </action>
  <verify>
    <automated>cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic && xcodebuild -scheme myFuckingMusic -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified the competitor station UI works correctly in the iOS Simulator and is only visible to STATION role users</done>
</task>

</tasks>

<verification>
```bash
# iOS project builds successfully
cd /Users/stefanpanaite/Documents/GitHub/myFuckingMusic/apps/ios/myFuckingMusic && xcodebuild -scheme myFuckingMusic -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5
```
</verification>

<success_criteria>
- STATION-role users see Competitor Stations in Settings; other roles do not
- Competitor list displays cards with station name, play count, top song
- Station picker allows browsing and adding competitors (up to 20)
- Swipe-to-delete removes competitors
- Competitor detail shows top songs (ranked), recent detections, and play count comparison
- Day/Week/Month segmented control switches data period
- Empty states are handled gracefully
- iOS project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-notifications-station-intelligence/09-04-SUMMARY.md`
</output>
